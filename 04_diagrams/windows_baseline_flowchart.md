# Windows Baseline Flowchart

```

Start baseline check
→ Check Windows version & patch level
→ Verify Defender AV
→ Verify BitLocker
→ Verify Firewall
→ Verify user privilege
→ Verify Security logs
→ Escalate if hardware/policy blocks baseline

```