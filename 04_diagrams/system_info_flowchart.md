# System Info Script Flowchart

```text
Start
→ Create timestamped folder
→ Detect admin? (warn if not)
→ Collect systeminfo + uptime
→ Collect CPU/RAM + disk usage
→ Collect top CPU & memory processes
→ Collect BitLocker / Defender / Firewall status
→ Write summary.txt
→ Optional zip

```
